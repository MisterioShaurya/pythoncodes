import os

# Define the command to run the script in a new terminal window
command = 'start cmd /k python chatbot.py'

# Open a new terminal window and run the script
os.system(command)
# Follow @codedepth for more! 
